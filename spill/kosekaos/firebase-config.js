// Optional local Firebase config for Kosekaos.
// Keep this file safe for git by not committing live API keys.
// In production, this is injected by GitHub Actions from ALMA_FIREBASE_CONFIG_JS secret.
// For local development without Firebase, this will remain null (uses localStorage only).
window.ALMA_FIREBASE_CONFIG = null;
