name: Deploy Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Prepare site artifact
        shell: bash
        run: |
          set -euo pipefail
          rm -rf site
          mkdir -p site/laering/gangetrappen site/spill/kosekaos
          git archive --format=tar HEAD | tar -xf - -C site

      - name: Inject optional runtime config
        shell: bash
        env:
          ALMA_FIREBASE_CONFIG_JS: ${{ secrets.ALMA_FIREBASE_CONFIG_JS }}
          ALMA_GA4_MEASUREMENT_ID: ${{ secrets.ALMA_GA4_MEASUREMENT_ID }}
        run: |
          set -euo pipefail
          if [ -z "${ALMA_FIREBASE_CONFIG_JS}" ]; then
            echo "⚠ Warning: ALMA_FIREBASE_CONFIG_JS secret not set, optional runtime config will be skipped" >&2
          else
            printf '%s\n' "$ALMA_FIREBASE_CONFIG_JS" > site/laering/gangetrappen/firebase-config.js
            printf '%s\n' "$ALMA_FIREBASE_CONFIG_JS" > site/spill/kosekaos/firebase-config.js
            echo "✓ Optional runtime config injected for gangetrappen and kosekaos"
          fi

          if [ -z "${ALMA_GA4_MEASUREMENT_ID}" ]; then
            printf '%s\n' "window.ALMA_GA4_MEASUREMENT_ID = null;" > site/analytics-config.js
            echo "⚠ Warning: ALMA_GA4_MEASUREMENT_ID secret not set, analytics disabled" >&2
          else
            printf "window.ALMA_GA4_MEASUREMENT_ID = '%s';\n" "$ALMA_GA4_MEASUREMENT_ID" > site/analytics-config.js
            echo "✓ GA4 Measurement ID injected"
          fi

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
